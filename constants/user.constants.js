export const USER_PROFILE_STATUS = Object.freeze({
  COMPLETE: "COMPLETE",
  INCOMPLETE: "INCOMPLETE"
});

export const USER_PROFILE_STATUS_ARRAY = Object.freeze(
  Object.values(USER_PROFILE_STATUS)
);


export const USER_ROLES = Object.freeze({
  ADMIN:"admin",
  USER: "user"
});

export const USER_ROLES_ARRAY = Object.freeze(
  Object.values(USER_ROLES)
);