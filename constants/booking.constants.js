export const BOOKING_STATUS =Object.freeze({
    PENDING: "pending", 
    CONFIRMED: "confirmed",
    CANCELLED: "cancelled", 
    COMPLETED: "completed"
});

export const BOOKING_STATUS_ARRAY = Object.freeze(Object.values(BOOKING_STATUS));